mutation UpdateArticle($id: Int!, $authorId: Int!, $name: String!, $description: String!) {
  updateArticle(input: { id: $id, authorId: $authorId, name: $name, description: $description }) {
    id,
    name,
    description  
  }
}